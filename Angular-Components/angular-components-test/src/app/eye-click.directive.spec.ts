import { EyeClickDirective } from './eye-click.directive';

describe('EyeClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EyeClickDirective();
    expect(directive).toBeTruthy();
  });
});
